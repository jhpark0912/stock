# 섹터 히트맵 기능 구현 계획

> **상태**: ✅ Phase 1 완료 (2026-02-07)
> **커밋**: `bba01d1` - :sparkles: [feat] 섹터 히트맵 기능 추가

## 1. 개요

### 목적
GICS 11개 섹터 ETF의 변화율을 히트맵으로 시각화하여 시장 섹터 로테이션을 한눈에 파악

### 핵심 결정사항
- **섹터 구성**: GICS 11개 섹터 전체 (XLK, XLF, XLV, XLE, XLI, XLB, XLY, XLP, XLRE, XLU, XLC)
- **기본 기간**: 1일 (1D), 1W/1M 전환 가능
- **배치**: Economics 탭 내부에 서브 탭으로 구성
  - `[경제 지표]` - 기존 기능
  - `[섹터 히트맵]` - 신규 기능

---

## 2. UI 구조

```
Economics 탭
├── 서브 탭: [경제 지표] [섹터 히트맵]
│
├── "경제 지표" 선택 시
│   └── 기존 EconomicIndicators (Simple/Chart 뷰 유지)
│
└── "섹터 히트맵" 선택 시
    └── SectorHeatmap 컴포넌트
        ├── 기간 선택: [1D] [1W] [1M]
        ├── 11개 섹터 그리드 (4열)
        └── 마지막 업데이트 시간
```

---

## 3. 구현 파일 목록

### Backend (신규/수정)
| 파일 | 작업 | 설명 |
|------|------|------|
| `backend/app/services/sector_service.py` | 신규 | 섹터 ETF 데이터 조회 + 캐싱 |
| `backend/app/models/economic.py` | 수정 | SectorData, SectorResponse 모델 추가 |
| `backend/app/api/routes/economic.py` | 수정 | `/api/economic/sectors` 엔드포인트 추가 |

### Frontend (신규/수정)
| 파일 | 작업 | 설명 |
|------|------|------|
| `frontend/src/components/economic/SectorHeatmap.tsx` | 신규 | 히트맵 UI 컴포넌트 |
| `frontend/src/components/economic/index.ts` | 수정 | export 추가 |
| `frontend/src/components/EconomicIndicators.tsx` | 수정 | 서브 탭 구조 추가 |
| `frontend/src/types/economic.ts` | 수정 | SectorData 타입 추가 |

---

## 4. Backend 상세 설계

### 4.1 데이터 모델 (`models/economic.py`)

```python
class SectorData(BaseModel):
    symbol: str          # ETF 심볼 (XLK)
    name: str            # 한글명 (기술)
    name_en: str         # 영문명 (Technology)
    price: float         # 현재가
    change_1d: float     # 일간 변화율 (%)
    change_1w: float     # 주간 변화율 (%)
    change_1m: float     # 월간 변화율 (%)

class SectorResponse(BaseModel):
    success: bool
    data: Optional[List[SectorData]] = None
    last_updated: Optional[str] = None
    error: Optional[str] = None
```

### 4.2 섹터 서비스 (`services/sector_service.py`)

```python
SECTOR_ETFS = {
    "XLK": {"name": "기술", "name_en": "Technology"},
    "XLF": {"name": "금융", "name_en": "Financials"},
    "XLV": {"name": "헬스케어", "name_en": "Healthcare"},
    "XLE": {"name": "에너지", "name_en": "Energy"},
    "XLI": {"name": "산업재", "name_en": "Industrials"},
    "XLB": {"name": "소재", "name_en": "Materials"},
    "XLY": {"name": "경기소비재", "name_en": "Consumer Cyclical"},
    "XLP": {"name": "필수소비재", "name_en": "Consumer Defensive"},
    "XLRE": {"name": "부동산", "name_en": "Real Estate"},
    "XLU": {"name": "유틸리티", "name_en": "Utilities"},
    "XLC": {"name": "커뮤니케이션", "name_en": "Communication Services"},
}

# 캐시 TTL: 5분 (기존 economic_service 패턴)
CACHE_TTL_SECTORS = 300
```

- yahooquery로 11개 ETF 병렬 조회
- 각 ETF의 1D/1W/1M 변화율 계산
- 메모리 캐시 (5분 TTL)

### 4.3 API 엔드포인트 (`routes/economic.py`)

```python
@router.get("/economic/sectors", response_model=SectorResponse)
async def get_sector_performance():
    """섹터 ETF 성과 데이터 조회"""
```

---

## 5. Frontend 상세 설계

### 5.1 EconomicIndicators 서브 탭 구조

```tsx
type EconomicTab = 'indicators' | 'sectors';

export function EconomicIndicators() {
  const [activeTab, setActiveTab] = useState<EconomicTab>('indicators');

  return (
    <div>
      {/* 서브 탭 헤더 */}
      <div className="flex gap-1 bg-muted rounded-lg p-1 mb-4">
        <button onClick={() => setActiveTab('indicators')}>
          경제 지표
        </button>
        <button onClick={() => setActiveTab('sectors')}>
          섹터 히트맵
        </button>
      </div>

      {/* 탭 컨텐츠 */}
      {activeTab === 'indicators' ? (
        /* 기존 경제 지표 UI */
      ) : (
        <SectorHeatmap />
      )}
    </div>
  );
}
```

### 5.2 SectorHeatmap 컴포넌트

```tsx
interface SectorData {
  symbol: string;
  name: string;
  name_en: string;
  price: number;
  change_1d: number;
  change_1w: number;
  change_1m: number;
}

type Period = '1D' | '1W' | '1M';

export function SectorHeatmap() {
  const [sectors, setSectors] = useState<SectorData[]>([]);
  const [period, setPeriod] = useState<Period>('1D');
  const [loading, setLoading] = useState(true);

  // 변화율에 따른 배경색
  const getChangeColor = (change: number) => {
    if (change >= 3) return 'bg-green-600';
    if (change >= 1) return 'bg-green-500';
    if (change >= 0) return 'bg-green-400/70';
    if (change >= -1) return 'bg-red-400/70';
    if (change >= -3) return 'bg-red-500';
    return 'bg-red-600';
  };

  return (
    <div className="p-6 space-y-6">
      {/* 헤더 + 기간 선택 */}
      {/* 4열 그리드 히트맵 */}
      {/* 마지막 업데이트 */}
    </div>
  );
}
```

### 5.3 히트맵 디자인 (옵션 A: 그리드 카드 스타일 선택)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  📊 섹터 로테이션                                    [1D] [1W] [1M]     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ ████████████ │  │ ████████████ │  │ ████████████ │  │ ████████████ │ │
│  │     XLK      │  │     XLF      │  │     XLV      │  │     XLE      │ │
│  │     기술     │  │     금융     │  │   헬스케어   │  │    에너지    │ │
│  │   +2.34%  ▲  │  │   -1.23%  ▼  │  │   +0.56%  ▲  │  │   +3.12%  ▲  │ │
│  │   $185.23    │  │   $42.15     │  │   $143.87    │  │   $89.56     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│   (진한 초록)        (연한 빨강)       (연한 초록)        (진한 초록)     │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │     XLI      │  │     XLB      │  │     XLY      │  │     XLP      │ │
│  │    산업재    │  │     소재     │  │  경기소비재  │  │  필수소비재  │ │
│  │   -0.45%  ▼  │  │   +1.78%  ▲  │  │   +0.89%  ▲  │  │   -0.12%  ▼  │ │
│  │   $115.42    │  │   $87.23     │  │   $178.56    │  │   $76.89     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                   │
│  │    XLRE      │  │     XLU      │  │     XLC      │                   │
│  │    부동산    │  │   유틸리티   │  │  커뮤니케이션│                   │
│  │   -2.01%  ▼  │  │   +0.23%  ▲  │  │   +1.45%  ▲  │                   │
│  │   $38.45     │  │   $67.12     │  │   $82.34     │                   │
│  └──────────────┘  └──────────────┘  └──────────────┘                   │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  마지막 업데이트: 2026-02-07 15:30:00 KST                    🔄 새로고침 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.4 개별 셀 구조

```
┌──────────────┐
│     XLK      │  ← 심볼 (볼드)
│     기술     │  ← 한글명
│   +2.34%  ▲  │  ← 변화율 + 방향 아이콘
│   $185.23    │  ← 현재가
└──────────────┘
```

### 5.5 색상 코딩

| 변화율 | 색상 | Tailwind 클래스 |
|--------|------|-----------------|
| +3% 이상 | 진한 초록 | `bg-green-600 text-white` |
| +1% ~ +3% | 초록 | `bg-green-500 text-white` |
| 0% ~ +1% | 연한 초록 | `bg-green-400/70 text-green-900` |
| 0% ~ -1% | 연한 빨강 | `bg-red-400/70 text-red-900` |
| -1% ~ -3% | 빨강 | `bg-red-500 text-white` |
| -3% 이하 | 진한 빨강 | `bg-red-600 text-white` |

### 5.6 레이아웃 사양

- **그리드**: 4열 (`grid-cols-4`)
- **반응형**: 모바일 2열, 태블릿 3열, 데스크탑 4열
- **카드 간격**: `gap-4`
- **카드 패딩**: `p-4`
- **카드 모서리**: `rounded-lg`

---

## 6. 캐싱 전략

| 항목 | 값 |
|------|-----|
| 저장 방식 | 메모리 캐시 (기존 패턴) |
| TTL | 5분 |
| 갱신 | 요청 기반 (캐시 만료 시) |
| DB 저장 | 불필요 (실시간 데이터) |

---

## 7. 구현 순서

### Phase 1: Backend ✅ 완료 (2026-02-07)
1. ✅ `models/economic.py`에 SectorData, SectorResponse 추가
2. ✅ `services/sector_service.py` 신규 생성
3. ✅ `routes/economic.py`에 `/economic/sectors` 추가
4. ✅ API 테스트

### Phase 2: Frontend ✅ 완료 (2026-02-07)
1. ✅ `components/economic/SectorHeatmap.tsx` 생성 (타입 내장)
2. ✅ `components/economic/index.ts` export 추가
3. ✅ `components/EconomicIndicators.tsx` 서브 탭 구조 추가

### Phase 3: 검증 ✅ 완료 (2026-02-07)
1. ✅ 전체 플로우 테스트
2. ✅ 반응형 레이아웃 확인
3. ✅ 로딩/에러 상태 확인

---

## 확장 Phase 2: 섹터 상세 (보유 종목) ✅ 완료 (2026-02-07)

### Backend
1. ✅ `models/economic.py`에 `SectorHolding`, `SectorHoldingsResponse` 추가
2. ✅ `services/sector_service.py`에 `get_sector_holdings()` 함수 추가 (24시간 캐시)
3. ✅ `routes/economic.py`에 `/api/economic/sectors/{symbol}/holdings` 추가

### Frontend
1. ✅ `components/economic/SectorDetail.tsx` 생성 (모달 컴포넌트)
2. ✅ `components/economic/SectorHeatmap.tsx` 클릭 핸들러 추가
3. ✅ `components/economic/index.ts` export 추가

---

## 8. 검증 방법

1. **Backend API 테스트**
   ```bash
   curl http://localhost:8000/api/economic/sectors
   ```
   - 11개 섹터 데이터 반환 확인
   - 변화율 값 유효성 확인

2. **Frontend 테스트**
   - Economics 탭 → 서브 탭 전환 동작
   - 히트맵 렌더링 및 색상 확인
   - 기간 (1D/1W/1M) 전환 동작

3. **캐싱 테스트**
   - 연속 요청 시 캐시 히트 확인
   - 5분 후 캐시 갱신 확인

---

## 9. 확장 기능: 섹터별 개별 종목 조회

### 9.1 개요

섹터 히트맵에서 특정 섹터 클릭 시, 해당 섹터 ETF의 상위 보유 종목을 조회하여 표시

### 9.2 기술적 가능성

yahooquery에서 ETF holdings 조회 지원:
```python
from yahooquery import Ticker

etf = Ticker('XLK')
holdings = etf.fund_top_holdings  # 상위 10개 보유 종목

# 반환 예시:
# Symbol | Name                  | Percent
# MSFT   | Microsoft Corporation | 12.5%
# AAPL   | Apple Inc.            | 8.3%
# NVDA   | NVIDIA Corporation    | 7.1%
```

### 9.3 UI 플로우

```
섹터 히트맵
    │
    ├── [XLK 기술] 클릭
    │       ↓
    └── 상위 종목 패널 (슬라이드/모달)
            ├── MSFT (12.5%) - 현재가, 변화율
            ├── AAPL (8.3%)  - 현재가, 변화율
            ├── NVDA (7.1%)  - 현재가, 변화율
            └── ... (상위 10개)
                    │
                    └── 개별 종목 클릭 → 주식 상세 페이지로 이동
```

### 9.4 추가 구현 파일

| 파일 | 작업 | 설명 |
|------|------|------|
| `backend/app/services/sector_service.py` | 수정 | `get_sector_holdings()` 함수 추가 |
| `backend/app/models/economic.py` | 수정 | `SectorHolding` 모델 추가 |
| `backend/app/api/routes/economic.py` | 수정 | `/api/economic/sectors/{symbol}/holdings` 추가 |
| `frontend/src/components/economic/SectorDetail.tsx` | 신규 | 섹터 상세 (보유 종목) 컴포넌트 |

### 9.5 데이터 모델 (확장)

```python
class SectorHolding(BaseModel):
    symbol: str          # 종목 심볼 (MSFT)
    name: str            # 종목명 (Microsoft Corporation)
    weight: float        # 비중 (12.5)
    price: Optional[float] = None      # 현재가
    change_1d: Optional[float] = None  # 일간 변화율

class SectorHoldingsResponse(BaseModel):
    success: bool
    sector_symbol: str   # ETF 심볼 (XLK)
    sector_name: str     # 섹터명 (기술)
    holdings: Optional[List[SectorHolding]] = None
    last_updated: Optional[str] = None
    error: Optional[str] = None
```

### 9.6 구현 우선순위

| 단계 | 기능 | 우선순위 |
|------|------|---------|
| **Phase 1 (현재)** | 섹터 히트맵 기본 기능 | 🔴 높음 |
| **Phase 2 (확장)** | 섹터 클릭 → 상위 종목 표시 | 🟡 중간 |
| **Phase 3 (확장)** | 개별 종목 클릭 → 주식 상세 | 🟢 낮음 (기존 기능 연결) |

### 9.7 캐싱 전략 (확장)

| 데이터 | TTL | 이유 |
|--------|-----|------|
| 섹터 변화율 | 5분 | 실시간성 필요 |
| 섹터 보유 종목 | 24시간 | 포트폴리오 구성 변경이 드묾 |
| 개별 종목 가격 | 5분 | 실시간성 필요 |

---

## 10. 전체 구현 로드맵

```
Phase 1: 섹터 히트맵 기본 (현재 계획)
├── Backend: 섹터 ETF 변화율 API
├── Frontend: 히트맵 그리드 + 기간 선택
└── 검증

Phase 2: 섹터 상세 (확장)
├── Backend: 섹터 보유 종목 API
├── Frontend: 섹터 클릭 → 상세 패널
└── 검증

Phase 3: 주식 연동 (확장)
├── 개별 종목 클릭 → 주식 상세 페이지
└── 포트폴리오 추가 기능 연동
```
