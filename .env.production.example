# ========================================
# 프로덕션 환경 변수 템플릿
# ========================================
# 사용법:
#   1. 이 파일을 .env로 복사: cp .env.production.example .env
#   2. 아래 값들을 실제 값으로 수정
#
# 배포 시나리오별 사용:
#   - GCP Cloud Build 배포: .env.gcp로 복사 후 수정
#   - SSL/HTTPS 배포: DOMAIN, SSL_EMAIL 설정 필수
#   - 일반 프로덕션: 기본 설정만 수정
# ========================================

# ========================================
# 🌐 서버 설정
# ========================================
SERVER_IP=0.0.0.0
ENVIRONMENT=production
LOG_LEVEL=INFO

# ========================================
# ☁️ GCP Cloud Build 설정 (선택)
# ========================================
# GCP Cloud Build로 이미지를 빌드하는 경우 필수
GCP_PROJECT_ID=your-gcp-project-id
REGION=us-central1
REPOSITORY=stock-app

# ========================================
# 🔒 SSL/HTTPS 설정 (선택)
# ========================================
# docker-compose.ssl.yml 사용 시 필수
# 무료 도메인 발급:
#   - Freenom: https://www.freenom.com (.tk, .ml, .ga, .cf, .gq)
#   - DuckDNS: https://www.duckdns.org (서브도메인)
#   - No-IP: https://www.noip.com (Dynamic DNS)
DOMAIN=example.com
SSL_EMAIL=admin@example.com

# ========================================
# 🔐 GCP Secret Manager (권장)
# ========================================
# true: 민감한 키를 Secret Manager에서 가져옴
#      (gemini-api-key, kis-app-key, jwt-secret-key, encryption-key, admin-password)
# false: .env 파일에서 직접 로드
#
# 로컬 개발: USE_SECRET_MANAGER=false
# GCP Cloud: USE_SECRET_MANAGER=true
USE_SECRET_MANAGER=true

# ========================================
# 🟢 낮은 보안 API 키 (무료 API)
# ========================================
# FRED API 키 (미국 경제지표, 선택)
# 발급: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# ECOS API 키 (한국은행 경제통계, 선택)
# 발급: https://ecos.bok.or.kr/
ECOS_API_KEY=your_ecos_api_key_here

# ========================================
# 🔴 높은 보안 키 (Secret Manager 권장)
# ========================================
# ⚠️  아래 키들은 프로덕션에서 Secret Manager 사용 권장
# USE_SECRET_MANAGER=false일 때만 .env에서 로드됨

# Gemini API 키 (AI 분석, 필수)
# 발급: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Secret (사용자 인증)
# 생성: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your_jwt_secret_here

# Encryption Key (API 키 암호화용, 필수)
# 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_key_here

# KIS API 키 (한국투자증권, Admin 전용)
# 발급: https://apiportal.koreainvestment.com
# 일반 유저는 설정 페이지에서 직접 입력
KIS_APP_KEY=your_kis_app_key_here
KIS_APP_SECRET=your_kis_app_secret_here

# Admin 기본 계정
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ========================================
# 🗄️ 데이터베이스 설정
# ========================================
DB_DIR=/app/data

# ========================================
# ⚙️ 기타 설정
# ========================================
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://frontend,http://localhost:5348,http://${SERVER_IP:-*},http://${SERVER_IP:-*}:5348
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
USE_MOCK_DATA=false
