# SSL/HTTPS 프로덕션 환경 설정
# 사용법:
#   1. 이 파일을 .env로 복사: cp .env.ssl.example .env
#   2. 아래 값들을 실제 값으로 수정
#   3. docker-compose -f docker-compose.yml -f docker-compose.ssl.yml up -d

# ========================================
# 도메인 설정 (필수)
# ========================================
# 무료 도메인 발급 사이트:
#   - Freenom: https://www.freenom.com (.tk, .ml, .ga, .cf, .gq)
#   - DuckDNS: https://www.duckdns.org (서브도메인 제공)
#   - No-IP: https://www.noip.com (Dynamic DNS)
DOMAIN=example.com

# SSL 인증서 발급용 이메일 (Let's Encrypt 알림 수신)
SSL_EMAIL=admin@example.com

# ========================================
# 서버 설정
# ========================================
# 서버 IP 주소 (클라우드 인스턴스의 공인 IP)
SERVER_IP=0.0.0.0

# 환경 모드
ENVIRONMENT=production

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ========================================
# GCP Secret Manager 설정 (프로덕션 권장)
# ========================================
# Secret Manager 사용 여부 (true/false)
# true: 민감한 키를 GCP Secret Manager에서 가져옴
# false: .env 파일에서 직접 로드
USE_SECRET_MANAGER=false

# GCP 프로젝트 ID (Secret Manager 사용 시 필수)
GCP_PROJECT_ID=your-gcp-project-id

# ========================================
# API 키 (낮은 보안 - .env에서 로드)
# ========================================
# FRED API 키 (미국 경제지표)
# 발급: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here

# ECOS API 키 (한국은행 경제통계)
# 발급: https://ecos.bok.or.kr/
ECOS_API_KEY=your_ecos_api_key_here

# ========================================
# 높은 보안 키 (Secret Manager 권장)
# ========================================
# 아래 키들은 USE_SECRET_MANAGER=true일 때 Secret Manager에서 가져옵니다.
# false일 때만 .env에서 로드됩니다.

# Gemini API 키 (AI 분석)
# 발급: https://aistudio.google.com/app/apikey
# ⚠️  프로덕션에서는 Secret Manager 사용 권장
GEMINI_API_KEY=your_gemini_api_key_here

# JWT Secret (사용자 인증 - 향후 구현 시)
# 생성: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_jwt_secret_here

# Encryption Key (데이터 암호화)
# 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_encryption_key_here

# KIS API 키 (한국투자증권 - 향후 구현 시)
# 발급: https://apiportal.koreainvestment.com
KIS_APP_KEY=your_kis_app_key_here
KIS_APP_SECRET=your_kis_app_secret_here

# ========================================
# 데이터베이스 설정
# ========================================
# 데이터베이스 디렉토리
DB_DIR=/app/data

# ========================================
# Docker 리소스 제한
# ========================================
# 메모리 제한은 docker-compose.yml에서 설정됨
# Backend: 1G / Frontend: 256M / Nginx: 256M / Certbot: 128M
